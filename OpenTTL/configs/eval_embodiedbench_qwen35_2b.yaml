defaults:
  - eval_embodiedbench
  - _self_

model:
  pretrained_model_name_or_path: /root/autodl-tmp/Qwen3.5-2B
  trust_remote_code: true
  attn_implementation: sdpa
  # EmbodiedBench 本地 Qwen3.5 路径在 eb_qwen35_transformers_local 中默认 enable_thinking=False；
  # 与下方一致；若需思考链请设环境变量 OPENTTL_QWEN35_ENABLE_THINKING=1
  chat_template_kwargs:
    enable_thinking: false
